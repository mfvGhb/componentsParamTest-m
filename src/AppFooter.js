

export default function AppFooter(props) {
  
  return(
          <div style={{height:'50px', backgroundColor: 'lightblue'}}>
             <h2>AppFooter {props.name}</h2>
          </div>
        );
  }
    